<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Modo Claro -->
    <style name="Base.Theme.Calcul" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="android:textColorPrimary">@color/my_light_text_color</item>
        <item name="android:windowBackground">@color/my_light_background</item>
    </style>

    <!-- Modo Noche -->
    <style name="Base.Theme.Calcul.Dark" parent="Base.Theme.Calcul">
        <item name="colorPrimary">@color/my_dark_primary</item>
        <item name="android:textColorPrimary">@color/my_dark_text_color</item>
        <item name="android:windowBackground">@color/my_dark_background</item>
    </style>

</resources>
